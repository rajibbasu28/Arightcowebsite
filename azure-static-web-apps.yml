- name: Build And Deploy
  uses: Azure/static-web-apps-deploy@v1
  with:
    azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
    app_location: "/" # or your actual app directory
    api_location: ""  # 👈 empty means: no API
    output_location: "out" # for static export builds
    skip_api_build: true  # 👈 skip the function build
